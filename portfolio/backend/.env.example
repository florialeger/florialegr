## Example environment file for local development
# Copy this to `backend/.env` and fill values before running the backend locally.

# MongoDB connection string (use MongoDB Atlas connection string format)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>

# Local server port
PORT=5000
MONGO_URI=<your-mongo-uri>
PORT=5000
